{
  "name": "jquery-plugin",
  "version": "0.0.0-ignored",
  "engines": {
    "node": ">= 0.6.0"
  },
  "scripts": {
    "test": "grunt qunit"
  },
  "devDependencies": {
    "grunt": "~0.3.15"
  },
  "dependencies": {
    "jQuery": "~1.7.3"
  },
  "scripts": {
    "test": "grunt travis --verbose"
  }
}